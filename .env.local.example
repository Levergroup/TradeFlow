# TradeFlow marketing site — environment variables
# Copy to .env.local for local dev. NEVER commit .env.local.
# Add all real values to Doppler AND Vercel env vars manually.

# Public — exposed to the browser
NEXT_PUBLIC_SITE_URL=https://www.tradeflow.io
NEXT_PUBLIC_APP_URL=https://app.tradeflow.io
NEXT_PUBLIC_GTM_ID=GTM-TRADEFLOW
NEXT_PUBLIC_GA4_ID=G-TRADEFLOW01

# Server-only — never exposed to the browser
GHL_WEBHOOK_URL=https://services.leadconnector.net/hooks/tradeflow-lead-capture
INDEXNOW_SECRET=
